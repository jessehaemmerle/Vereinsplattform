services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: verein_docker
    ports:
      - "5000:5000"
    volumes:
      - databases_volume:/app/databases
      - uploads_volume:/app/uploads

volumes:
  databases_volume:
  uploads_volume:
